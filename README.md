# 🔍 SergD - SERP XMLRiver Collector - Сборщик поисковой выдачи

## 👨‍💻 Автор

SergD https://t.me/sergei_dyshkant

## 📋 Описание

Скрипт для сбора данных по запросам о банкротстве физических лиц из поисковых систем Яндекс и Google с использованием API XMLRiver. Обеспечивает параллельный сбор данных с контролем количества одновременных запросов, обработкой ошибок и автоматическим сохранением результатов.

## ✨ Основные возможности

- 🔍 Сбор данных из Яндекса и Google по ключевым запросам о банкротстве
- 🏙️ Поддержка поиска по Москве и городам Московской области
- 📝 Загрузка ключевых слов из файла
- 🔄 Параллельная обработка запросов с ограничением нагрузки
- 💾 Автоматическое промежуточное сохранение результатов
- 📊 Создание CSV-отчета для анализа
- ⚠️ Надежная обработка ошибок и повторные попытки запросов

## 🧲 Структура проекта

### 📂 Файлы
- **serp-collector-bfl-enhanced.js** - основной скрипт сбора данных
- **keywords_bfl.txt** - файл с ключевыми словами для поиска
- **src/xmlriver-api.js** - модуль для работы с API XMLRiver
- **.env** - файл с переменными окружения и API-ключами

### 💶 Зависимости
```
npm install dotenv fs-extra axios xml2js
```

## 🚀 Как использовать

### 1️⃣ Подготовка
1. Установите Node.js (версия 14+ рекомендуется)
2. Установите зависимости: `npm install dotenv fs-extra axios xml2js`
3. Создайте файл `.env` с вашими учетными данными XMLRiver:
   ```
   XMLRIVER_USER_ID=ваш_идентификатор
   XMLRIVER_API_KEY=ваш_ключ_api
   ```

### 2️⃣ Настройка ключевых слов
Создайте файл `keywords_bfl.txt` в корне проекта с ключевыми словами, по одному на строку:
```
банкротство физических лиц
закон о банкротстве
банкротство при разводе
...
```

**Примечания:**
- Строки, начинающиеся с `#`, считаются комментариями и игнорируются
- Если файл не найден, будет использован встроенный список запросов

### 3️⃣ Запуск скрипта
```bash
node serp-collector-bfl-enhanced.js
```

## 💡 Принцип работы

1. **Инициализация**:
   - Загрузка переменных окружения из `.env`
   - Подключение к API XMLRiver
   - Загрузка ключевых слов из файла

2. **Сбор данных**:
   - Очередь задач с ограничением параллельных запросов
   - Запросы к Яндексу и Google для каждой комбинации город+запрос
   - Промежуточное сохранение каждые 5 задач

3. **Финальное сохранение**:
   - JSON-файл с полными данными
   - CSV-отчет для анализа

## 💾 Результаты работы

- Результаты сохраняются в директории `data/results/bfl/`
- JSON-файлы: `bfl_results_[timestamp].json` 
- CSV-отчеты: `bfl_results_[timestamp].csv`
- Промежуточные: `intermediate_results_[N]_of_[total].json`

## 🔒 Лицензия
Права никакие не защищены )

---

© 2025 SergD https://t.me/sergei_dyshkant
- 🔄 Сбор до 100 уникальных результатов для каждого ключевого слова
- 📊 Поддержка Яндекса и Google
- 📍 Учет географического таргетинга (более 50 крупных городов России)
- 📅 Фильтрация результатов по временному периоду (за месяц)
- 🧹 Удаление дублирующихся результатов
- 💾 Сохранение отчетов в JSON формате с подробной статистикой

## 🚀 Установка и настройка

### Предварительные требования

- Node.js 14+ 
- API ключ от сервиса XMLRiver (платный)

### Установка

1. Клонируйте репозиторий:
```
git clone <репозиторий>
cd SergD_SERP_APIXMLRiver
```

2. Установите зависимости:
```
npm install
```

3. Настройте `.env` файл:
```
XMLRIVER_USER_ID=ваш_идентификатор
XMLRIVER_API_KEY=ваш_ключ
```

## 📈 Использование

### Запуск сбора данных поисковой выдачи

```
node serp-collector.js
```

### Оценка потенциального трафика

```
node traffic-estimator.js
```

### Сбор уникальных URL

```
node url-collector.js
```

### Очистка и фильтрация доменов

```
node domain-cleaner.js
```

### Тестирование API Wordstat

```
node test-wordstat-new-fixed.js
node test-wordstat.js
```

### Настройка параметров

Основные параметры можно изменить в файле `serp-collector.js` в объекте `CONFIG`:

- `targetUniqueResults`: целевое количество уникальных результатов (по умолчанию 100)
- `maxPages`: максимальное количество страниц для сбора (по умолчанию 15)
- `pageSize`: количество результатов на странице (по умолчанию 10)
- `within`: период времени для фильтрации (2 = месяц)
- `filter`: фильтрация похожих результатов (1 = включена)

### Ключевые фразы

Отредактируйте файл `keywords.txt`, чтобы изменить список ключевых фраз для сбора.

## 📁 Структура проекта

```
.
├── serp-collector.js     # Основной скрипт сбора данных из поисковой выдачи
├── traffic-estimator.js  # Скрипт оценки потенциального трафика на URL
├── url-collector.js      # Сборщик уникальных URL из результатов
├── domain-cleaner.js     # Скрипт для очистки и фильтрации доменов
├── src/
│   └── xmlriver-api.js   # Модуль для работы с XMLRiver API
├── data/
│   ├── results/          # Директория для результатов
│   └── debug/            # Отладочная информация
├── keywords.txt          # Файл с ключевыми фразами
├── .env                  # Переменные окружения
└── package.json          # Зависимости проекта
```

## 📝 Формат результатов

### Результаты поисковой выдачи

Для каждого города и ключевого слова создается отдельный JSON файл, содержащий:

- Метаинформацию (город, ключевое слово, время сбора)
- Результаты Яндекса (до 100 уникальных URL)
- Результаты Google (до 100 уникальных URL)
- Позиции, заголовки, описания и URL для каждого результата

Итоговый отчет содержит агрегированные данные по всем городам и ключевым словам.

### Результаты оценки трафика

Скрипт traffic-estimator.js создает файлы:

- `traffic_results.json` - детальные данные по оценке трафика для каждого URL
- `traffic_summary.json` - сводная информация по всем обработанным URL
- `cache.json` - кэш частотности запросов из Wordstat API
- `progress.json` - текущий прогресс обработки для возможности продолжения

## ⚙️ Дополнительные настройки

### Управление городами

Список городов можно изменить в объекте `MAJOR_CITIES` в файле `serp-collector.js`. Для каждого города указан его код в системе Яндекса.

### Параметры API

Настройки API запросов можно изменить в методе `getUniqueResults` файла `serp-collector.js`.

## 📊 Производительность

- ⏱️ Время сбора: ~5-10 секунд на ключевую фразу (зависит от настроек)
- 💸 Расход API: ~2-3 запроса на ключевую фразу и город
- 📈 Общее количество запросов: ~100 ключевых фраз × 50 городов × 2 поисковые системы × 2-3 запроса = ~20,000-30,000 запросов

## 🔧 Решение проблем

### Ошибка авторизации
Проверьте корректность учетных данных в `.env` файле.

### Нет результатов
Убедитесь, что баланс на аккаунте XMLRiver положительный.

### Блокировка API
При большом количестве запросов может возникнуть временная блокировка. Увеличьте значение `delayBetweenRequests` в конфигурации.

### Таймаут API (2025-05-22)
В настоящее время наблюдаются проблемы со стороны API XMLRiver - запросы заканчиваются таймаутом. Возможные решения:

1. Увеличить значение таймаута в настройках (requestTimeout в XmlRiverApi)
2. Проверить статус сервиса на официальном сайте
3. Обратиться в службу поддержки XMLRiver
4. Временно приостановить сбор данных до восстановления работы сервиса

### Ошибки в traffic-estimator.js
Если скрипт оценки трафика завершается с ошибками:

1. Проверьте файл cache.json на корректность
2. Убедитесь, что файл progress.json существует и имеет правильный формат
3. Проверьте доступность Wordstat API через тестовые скрипты
